A small uppercase mono label — agent objectives, statuses, metadata.

```jsx
<Chip accent="blue">Collections & Recovery</Chip>
<Chip accent="emerald">Sales Qualification</Chip>
```
String children are auto-uppercased. Glassy by default (faint fill, accent border + text).
