Multi-line input for free-form feedback. All native `<textarea>` props pass through.

```jsx
<Textarea placeholder="What went well? What could be better?" value={text} onChange={e => setText(e.target.value)} />
```
